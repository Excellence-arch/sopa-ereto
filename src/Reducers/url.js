const initState = {
  baseUrl: 'https://sopa-ereto'
};

const url = (state = initState, action) => {
  switch (action.type) {
    default:
      return {
        ...state
      };
  }
};

export default url;
