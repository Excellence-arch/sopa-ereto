const initState = {
  baseUrl: 'https://sopa-ereto-diam.herokuapp.com/'
};

const url = (state = initState, action) => {
  switch (action.type) {
    default:
      return {
        ...state
      };
  }
};

export default url;
