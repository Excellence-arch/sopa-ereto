const initState = {
  status: false,
  id: ''
};

const online = (state = initState, action) => {
  switch (action.type) {
    default:
      return {
        ...state
      };
  }
};

export default online;
